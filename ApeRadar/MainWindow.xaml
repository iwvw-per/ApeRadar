<Window x:Class="ApeRadar.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ApeRadar"
        xmlns:utils="clr-namespace:ApeRadar.Utils"
        xmlns:cvt="clr-namespace:ApeRadar.Utils.Converters"
        xmlns:model="clr-namespace:ApeRadar.Models"
        xmlns:p="clr-namespace:ApeRadar.Properties"
        xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
        mc:Ignorable="d"
        Tag="{DynamicResource WindowTitleApeRadar}" ResizeMode="CanResize" AllowDrop="True" Drop="Window_Drop" WindowStartupLocation="CenterScreen" WindowState="Maximized" UseLayoutRounding="True">
    <Window.Title>
        <MultiBinding StringFormat="{}{0} {1} Build {2}">
            <Binding RelativeSource="{RelativeSource Self}" Path="Tag"/>
            <Binding Source="{x:Static utils:BuildInfo.SoftwareVersion}"/>
            <Binding Source="{x:Static utils:BuildInfo.SoftwareDate}"/>
        </MultiBinding>
    </Window.Title>
    <Window.Resources>
        <cvt:ShipTierConverter x:Key="CvtShipTier"/>
        <cvt:BattleDataAvailabilityConverter x:Key="CvtBattleData"/>
        <cvt:TagDataConverter x:Key="CvtTagData"/>
        <cvt:TagBackgroundColorConverter x:Key="CvtBackgroundColor"/>
        <cvt:DataGridColumnVisibilityConverter x:Key="CvtDataGridColumnVisibility"/>
        <cvt:DataGridColumnFadedColorConverter x:Key="CvtDataGridColumnFadedColor"/>
        <cvt:LabelGamePathIsSetContentConverter x:Key="CvtGamePathIsSetText"/>
        <cvt:LabelGamePathIsSetColorConverter x:Key="CvtGamePathIsSetColor"/>
        <cvt:ContextMenuItemCopyPlayerStatisticsIsEnabledConverter x:Key="CvtContextMenuItemCopyPlayerStatisticsIsEnabled"/>
        <cvt:ContextMenuItemAddToWatchListPositiveIsEnabledConverter x:Key="CvtContextMenuItemAddToWatchListPositiveIsEnabled"/>
        <cvt:ContextMenuItemAddToWatchListNegtiveIsEnabledConverter x:Key="CvtContextMenuItemAddToWatchListNegtiveIsEnabled"/>
        <cvt:ContextMenuItemAddToWatchListCheaterIsEnabledConverter x:Key="CvtContextMenuItemAddToWatchListCheaterIsEnabled"/>
        <cvt:ContextMenuItemRemoveFromWatchListIsEnabledConverter x:Key="CvtContextMenuItemRemoveFromWatchListIsEnabled"/>
        <cvt:ContextMenuItemCheckOnWoWSOfficialSiteIsEnabledConverter x:Key="CvtContextMenuItemCheckOnWoWSOfficialSiteIsEnabled"/>
        <cvt:ContextMenuItemCheckOnWoWSNumbersIsEnabledConverter x:Key="CvtContextMenuItemCheckOnWoWSNumbersIsEnabled"/>
        <cvt:PlayerNamesVisibilityConverter x:Key="CvtPlayerNamesVisibility"/>
        <cvt:WatchStatusIconConverter x:Key="CvtWatchStatusIcon"/>
        <cvt:WatchStatusColorConverter x:Key="CvtWatchStatusColor"/>
        <cvt:NotificationMessageColorConverter x:Key="CvtNotificationMessageColor"/>
        
        <ToolTip x:Key="ToolTipStatisticsLegend">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid Grid.Row="0" Grid.Column="0">
                    <StackPanel Orientation="Vertical" Width="90" Visibility="{Binding Source={x:Static p:Settings.Default}, Path=AccountWinrateVisibility, Converter={StaticResource CvtDataGridColumnVisibility}}">
                        <TextBlock Text="{DynamicResource DataGridToolTipAccount}" HorizontalAlignment="Center" FontSize="15" Margin="5"/>
                        <TextBlock Text="{DynamicResource DataGridToolTipBattles}" HorizontalAlignment="Center" FontSize="15" Margin="5,5,5,0"/>
                        <TextBlock Text="{DynamicResource DataGridToolTipWinrate}" HorizontalAlignment="Center" FontSize="15" Margin="5,0,5,0"/>
                        <TextBlock Text="{DynamicResource DataGridToolTipAvgExp}" HorizontalAlignment="Center" FontSize="15" Margin="5,0,5,5" Visibility="{Binding Source={x:Static p:Settings.Default}, Path=AccountAvgExpVisibility, Converter={StaticResource CvtDataGridColumnVisibility}}"/>
                    </StackPanel>
                </Grid>
                <Grid Grid.Row="0" Grid.Column="1">
                    <StackPanel Orientation="Vertical" Width="90" Visibility="{Binding Source={x:Static p:Settings.Default}, Path=WeightedWinrateVisibility, Converter={StaticResource CvtDataGridColumnVisibility}}">
                        <TextBlock Text="{DynamicResource DataGridToolTipWeighted}" HorizontalAlignment="Center" FontSize="15" Margin="5"/>
                        <TextBlock Text="" HorizontalAlignment="Center" FontSize="15" Margin="5,5,5,0"/>
                        <TextBlock Text="{DynamicResource DataGridToolTipWinrate}" HorizontalAlignment="Center" FontSize="15" Margin="5,0,5,5"/>
                    </StackPanel>
                </Grid>
                <Grid Grid.Row="0" Grid.Column="2">
                    <StackPanel Orientation="Vertical" Width="90" Visibility="{Binding Source={x:Static p:Settings.Default}, Path=ShipWinrateVisibility, Converter={StaticResource CvtDataGridColumnVisibility}}">
                        <TextBlock Text="{DynamicResource DataGridToolTipShip}" HorizontalAlignment="Center" FontSize="15" Margin="5"/>
                        <TextBlock Text="{DynamicResource DataGridToolTipBattles}" HorizontalAlignment="Center" FontSize="15" Margin="5,5,5,0"/>
                        <TextBlock Text="{DynamicResource DataGridToolTipWinrate}" HorizontalAlignment="Center" FontSize="15" Margin="5,0,5,0"/>
                        <TextBlock Text="{DynamicResource DataGridToolTipAvgExp}" HorizontalAlignment="Center" FontSize="15" Margin="5,0,5,0" Visibility="{Binding Source={x:Static p:Settings.Default}, Path=ShipAvgExpVisibility, Converter={StaticResource CvtDataGridColumnVisibility}}"/>
                        <TextBlock Text="{DynamicResource DataGridToolTipAvgDmg}" HorizontalAlignment="Center" FontSize="15" Margin="5,0,5,5" Visibility="{Binding Source={x:Static p:Settings.Default}, Path=ShipAvgDmgVisibility, Converter={StaticResource CvtDataGridColumnVisibility}}"/>
                    </StackPanel>
                </Grid>
            </Grid>
        </ToolTip>
        <Style x:Key="DataGridStatisticsColumnHeaderStyle" TargetType="DataGridColumnHeader" BasedOn="{StaticResource {x:Type DataGridColumnHeader}}">
            <Setter Property="ToolTip" Value="{StaticResource ToolTipStatisticsLegend}"/>
        </Style>

        <ToolTip x:Key="ToolTipStatisticsLegendMirrored">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid Grid.Row="0" Grid.Column="0">
                    <StackPanel Orientation="Vertical" Width="90" Visibility="{Binding Source={x:Static p:Settings.Default}, Path=ShipWinrateVisibility, Converter={StaticResource CvtDataGridColumnVisibility}}">
                        <TextBlock Text="{DynamicResource DataGridToolTipShip}" HorizontalAlignment="Center" FontSize="15" Margin="5"/>
                        <TextBlock Text="{DynamicResource DataGridToolTipBattles}" HorizontalAlignment="Center" FontSize="15" Margin="5,5,5,0"/>
                        <TextBlock Text="{DynamicResource DataGridToolTipWinrate}" HorizontalAlignment="Center" FontSize="15" Margin="5,0,5,0"/>
                        <TextBlock Text="{DynamicResource DataGridToolTipAvgExp}" HorizontalAlignment="Center" FontSize="15" Margin="5,0,5,0" Visibility="{Binding Source={x:Static p:Settings.Default}, Path=ShipAvgExpVisibility, Converter={StaticResource CvtDataGridColumnVisibility}}"/>
                        <TextBlock Text="{DynamicResource DataGridToolTipAvgDmg}" HorizontalAlignment="Center" FontSize="15" Margin="5,0,5,5" Visibility="{Binding Source={x:Static p:Settings.Default}, Path=ShipAvgDmgVisibility, Converter={StaticResource CvtDataGridColumnVisibility}}"/>
                    </StackPanel>
                </Grid>
                <Grid Grid.Row="0" Grid.Column="1">
                    <StackPanel Orientation="Vertical" Width="90" Visibility="{Binding Source={x:Static p:Settings.Default}, Path=WeightedWinrateVisibility, Converter={StaticResource CvtDataGridColumnVisibility}}">
                        <TextBlock Text="{DynamicResource DataGridToolTipWeighted}" HorizontalAlignment="Center" FontSize="15" Margin="5"/>
                        <TextBlock Text="" HorizontalAlignment="Center" FontSize="15" Margin="5,5,5,0"/>
                        <TextBlock Text="{DynamicResource DataGridToolTipWinrate}" HorizontalAlignment="Center" FontSize="15" Margin="5,0,5,5"/>
                    </StackPanel>
                </Grid>
                <Grid Grid.Row="0" Grid.Column="2">
                    <StackPanel Orientation="Vertical" Width="90" Visibility="{Binding Source={x:Static p:Settings.Default}, Path=AccountWinrateVisibility, Converter={StaticResource CvtDataGridColumnVisibility}}">
                        <TextBlock Text="{DynamicResource DataGridToolTipAccount}" HorizontalAlignment="Center" FontSize="15" Margin="5"/>
                        <TextBlock Text="{DynamicResource DataGridToolTipBattles}" HorizontalAlignment="Center" FontSize="15" Margin="5,5,5,0"/>
                        <TextBlock Text="{DynamicResource DataGridToolTipWinrate}" HorizontalAlignment="Center" FontSize="15" Margin="5,0,5,0"/>
                        <TextBlock Text="{DynamicResource DataGridToolTipAvgExp}" HorizontalAlignment="Center" FontSize="15" Margin="5,0,5,5" Visibility="{Binding Source={x:Static p:Settings.Default}, Path=AccountAvgExpVisibility, Converter={StaticResource CvtDataGridColumnVisibility}}"/>
                    </StackPanel>
                </Grid>
            </Grid>
        </ToolTip>
        <Style x:Key="DataGridStatisticsColumnHeaderMirroredStyle" TargetType="DataGridColumnHeader" BasedOn="{StaticResource {x:Type DataGridColumnHeader}}">
            <Setter Property="ToolTip" Value="{StaticResource ToolTipStatisticsLegendMirrored}"/>
        </Style>

        <DataTemplate x:Key="NameHeaderTemplate">
            <TextBlock Text="{DynamicResource DataGridHeaderPlayer}" FontSize="15"/>
        </DataTemplate>
        <DataTemplate x:Key="NameTemplate">
            <StackPanel Orientation="Vertical" VerticalAlignment="Center" >
                <TextBlock FontSize="{Binding Source={x:Static p:Settings.Default}, Path=PlayerColumnFontSize}" Margin="5,0,5,0" Padding="0,-5,0,0" Foreground="{Binding WatchStatus, Converter={StaticResource CvtWatchStatusColor}}" TextTrimming="CharacterEllipsis" ToolTip="{Binding Name}">
                    <Run Text="{Binding ClanTag, Converter={StaticResource CvtPlayerNamesVisibility}}"/> <Run Text="{Binding Name, Converter={StaticResource CvtPlayerNamesVisibility}}"/> <Run BaselineAlignment="Superscript" FontSize="12" Text="{Binding Karma, Converter={StaticResource CvtBattleData}}"/><Run Text="{Binding WatchStatus, Converter={StaticResource CvtWatchStatusIcon}}"/>
                </TextBlock>
                <TextBlock FontSize="{Binding Source={x:Static p:Settings.Default}, Path=PlayerColumnFontSize}" Margin="5,0,5,0" Padding="0,0,0,-5" Foreground="{Binding WatchStatus, Converter={StaticResource CvtWatchStatusColor}}" TextTrimming="CharacterEllipsis" ToolTip="{Binding ShipName}">
                    <Run Text="{Binding ShipTier, Converter={StaticResource CvtShipTier}}"/> <Run Text="{Binding ShipName}"/>
                </TextBlock>
            </StackPanel>
        </DataTemplate>
        <DataTemplate x:Key="StatisticsHeaderTemplate">
            <Grid Margin="-4,0,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid Grid.Row="0" Grid.Column="0">
                    <StackPanel Orientation="Vertical" Width="80" Visibility="{Binding Source={x:Static p:Settings.Default}, Path=AccountWinrateVisibility, Converter={StaticResource CvtDataGridColumnVisibility}}">
                        <TextBlock Text="{DynamicResource DataGridHeaderStatisticsAccount}" HorizontalAlignment="Center" FontSize="15"/>
                    </StackPanel>
                </Grid>
                <Grid Grid.Row="0" Grid.Column="1">
                    <StackPanel Orientation="Vertical" Width="80" Visibility="{Binding Source={x:Static p:Settings.Default}, Path=WeightedWinrateVisibility, Converter={StaticResource CvtDataGridColumnVisibility}}">
                        <TextBlock Text="{DynamicResource DataGridHeaderStatisticsWeighted}" HorizontalAlignment="Center" FontSize="15"/>
                    </StackPanel>
                </Grid>
                <Grid Grid.Row="0" Grid.Column="2">
                    <StackPanel Orientation="Vertical" Width="80" Visibility="{Binding Source={x:Static p:Settings.Default}, Path=ShipWinrateVisibility, Converter={StaticResource CvtDataGridColumnVisibility}}">
                        <TextBlock Text="{DynamicResource DataGridHeaderStatisticsShip}" HorizontalAlignment="Center" FontSize="15"/>
                    </StackPanel>
                </Grid>
            </Grid>
        </DataTemplate>
        <DataTemplate x:Key="StatisticsTemplate">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid Grid.Row="0" Grid.Column="0">
                    <StackPanel Orientation="Vertical" Width="80" Visibility="{Binding Source={x:Static p:Settings.Default}, Path=AccountWinrateVisibility, Converter={StaticResource CvtDataGridColumnVisibility}}">
                        <TextBlock Text="{Binding Battles, Converter={StaticResource CvtBattleData}}" Foreground="{Binding Source={x:Static p:Settings.Default}, Path=AccountWinrateVisibility, Converter={StaticResource CvtDataGridColumnFadedColor}}" HorizontalAlignment="Center" FontSize="{Binding Source={x:Static p:Settings.Default}, Path=StatisticsColumnFontSize}" Margin="5,0,5,0"/>
                        <TextBlock Text="{Binding AccountWinrate, Converter={StaticResource CvtBattleData}, StringFormat={}{0:p2}}" Foreground="{Binding Source={x:Static p:Settings.Default}, Path=AccountWinrateVisibility, Converter={StaticResource CvtDataGridColumnFadedColor}}" HorizontalAlignment="Center" FontSize="{Binding Source={x:Static p:Settings.Default}, Path=StatisticsColumnFontSize}" Margin="5,0,5,0"/>
                        <TextBlock Text="{Binding AvgExpPerBattle, Converter={StaticResource CvtBattleData}, StringFormat={}{0:f0}}" Foreground="{Binding Source={x:Static p:Settings.Default}, Path=AccountAvgExpVisibility, Converter={StaticResource CvtDataGridColumnFadedColor}}" HorizontalAlignment="Center" FontSize="{Binding Source={x:Static p:Settings.Default}, Path=StatisticsColumnFontSize}" Margin="5,0,5,0" Visibility="{Binding Source={x:Static p:Settings.Default}, Path=AccountAvgExpVisibility, Converter={StaticResource CvtDataGridColumnVisibility}}"/>
                    </StackPanel>
                </Grid>
                <Grid Grid.Row="0" Grid.Column="1">
                    <StackPanel Orientation="Vertical" Width="80" Visibility="{Binding Source={x:Static p:Settings.Default}, Path=WeightedWinrateVisibility, Converter={StaticResource CvtDataGridColumnVisibility}}">
                        <TextBlock Text="" HorizontalAlignment="Center" FontSize="{Binding Source={x:Static p:Settings.Default}, Path=StatisticsColumnFontSize}" Margin="5,0,5,0"/>
                        <TextBlock Text="{Binding WeightedWinrate, Converter={StaticResource CvtBattleData}, StringFormat={}{0:p2}}" Foreground="{Binding Source={x:Static p:Settings.Default}, Path=WeightedWinrateVisibility, Converter={StaticResource CvtDataGridColumnFadedColor}}" HorizontalAlignment="Center" FontSize="{Binding Source={x:Static p:Settings.Default}, Path=StatisticsColumnFontSize}" Margin="5,0,5,0"/>
                    </StackPanel>
                </Grid>
                <Grid Grid.Row="0" Grid.Column="2">
                    <StackPanel Orientation="Vertical" Width="80" Visibility="{Binding Source={x:Static p:Settings.Default}, Path=ShipWinrateVisibility, Converter={StaticResource CvtDataGridColumnVisibility}}">
                        <TextBlock Text="{Binding ShipBattles, Converter={StaticResource CvtBattleData}}" Foreground="{Binding Source={x:Static p:Settings.Default}, Path=ShipWinrateVisibility, Converter={StaticResource CvtDataGridColumnFadedColor}}" HorizontalAlignment="Center" FontSize="{Binding Source={x:Static p:Settings.Default}, Path=StatisticsColumnFontSize}" Margin="5,0,5,0"/>
                        <TextBlock Text="{Binding ShipWinrate, Converter={StaticResource CvtBattleData}, StringFormat={}{0:p2}}" Foreground="{Binding Source={x:Static p:Settings.Default}, Path=ShipWinrateVisibility, Converter={StaticResource CvtDataGridColumnFadedColor}}" HorizontalAlignment="Center" FontSize="{Binding Source={x:Static p:Settings.Default}, Path=StatisticsColumnFontSize}" Margin="5,0,5,0"/>
                        <TextBlock Text="{Binding ShipAvgExpPerBattle, Converter={StaticResource CvtBattleData}, StringFormat={}{0:f0}}" Foreground="{Binding Source={x:Static p:Settings.Default}, Path=ShipAvgExpVisibility, Converter={StaticResource CvtDataGridColumnFadedColor}}" HorizontalAlignment="Center" FontSize="{Binding Source={x:Static p:Settings.Default}, Path=StatisticsColumnFontSize}" Margin="5,0,5,0" Visibility="{Binding Source={x:Static p:Settings.Default}, Path=ShipAvgExpVisibility, Converter={StaticResource CvtDataGridColumnVisibility}}"/>
                        <TextBlock Text="{Binding ShipAvgDmgPerBattle, Converter={StaticResource CvtBattleData}, StringFormat={}{0:f0}}" Foreground="{Binding Source={x:Static p:Settings.Default}, Path=ShipAvgDmgVisibility, Converter={StaticResource CvtDataGridColumnFadedColor}}" HorizontalAlignment="Center" FontSize="{Binding Source={x:Static p:Settings.Default}, Path=StatisticsColumnFontSize}" Margin="5,0,5,0" Visibility="{Binding Source={x:Static p:Settings.Default}, Path=ShipAvgDmgVisibility, Converter={StaticResource CvtDataGridColumnVisibility}}"/>
                    </StackPanel>
                </Grid>
            </Grid>
        </DataTemplate>
        <DataTemplate x:Key="TagHeaderTemplate">
            <TextBlock Text="{DynamicResource DataGridHeaderTag}" TextAlignment="Center" FontSize="15" Visibility="{Binding Source={x:Static p:Settings.Default}, Path=TagVisibility, Converter={StaticResource CvtDataGridColumnVisibility}}"/>
        </DataTemplate>
        <DataTemplate x:Key="TagTemplate">
            <local:CenteredGlyph FontSize="30" Foreground="{Binding Source={x:Static p:Settings.Default}, Path=TagVisibility, Converter={StaticResource CvtDataGridColumnFadedColor}}">
                <local:CenteredGlyph.Text>
                    <MultiBinding Converter="{StaticResource CvtTagData}">
                        <Binding Path="AccountWinrate"/>
                        <Binding Path="WeightedWinrate"/>
                        <Binding Path="Battles"/>
                        <Binding Path="IsHidden"/>
                    </MultiBinding>
                </local:CenteredGlyph.Text>
            </local:CenteredGlyph>
        </DataTemplate>

        <DataTemplate x:Key="NameTemplateMirrored">
            <StackPanel Orientation="Vertical" VerticalAlignment="Center">
                <TextBlock FontSize="{Binding Source={x:Static p:Settings.Default}, Path=PlayerColumnFontSize}" Margin="5,0,5,0" Padding="0,-5,0,0" HorizontalAlignment="Right">
                    <Run Text="{Binding ClanTag, Converter={StaticResource CvtPlayerNamesVisibility}}"/> <Run Text="{Binding Name, Converter={StaticResource CvtPlayerNamesVisibility}}"/> <Run BaselineAlignment="Superscript" FontSize="12" Text="{Binding Karma, Converter={StaticResource CvtBattleData}}"/>
                </TextBlock>
                <TextBlock FontSize="{Binding Source={x:Static p:Settings.Default}, Path=PlayerColumnFontSize}" Margin="5,0,5,0" Padding="0,0,0,-5" HorizontalAlignment="Right">
                    <Run Text="{Binding ShipTier, Converter={StaticResource CvtShipTier}}"/> <Run Text="{Binding ShipName}"/>
                </TextBlock>
            </StackPanel>
        </DataTemplate>
        <DataTemplate x:Key="StatisticsHeaderTemplateMirrored">
            <Grid Margin="-4,0,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid Grid.Row="0" Grid.Column="0">
                    <StackPanel Orientation="Vertical" Width="80" Visibility="{Binding Source={x:Static p:Settings.Default}, Path=ShipWinrateVisibility, Converter={StaticResource CvtDataGridColumnVisibility}}">
                        <TextBlock Text="{DynamicResource DataGridHeaderStatisticsShip}" HorizontalAlignment="Center" FontSize="15"/>
                    </StackPanel>
                </Grid>
                <Grid Grid.Row="0" Grid.Column="1">
                    <StackPanel Orientation="Vertical" Width="80" Visibility="{Binding Source={x:Static p:Settings.Default}, Path=WeightedWinrateVisibility, Converter={StaticResource CvtDataGridColumnVisibility}}">
                        <TextBlock Text="{DynamicResource DataGridHeaderStatisticsWeighted}" HorizontalAlignment="Center" FontSize="15"/>
                    </StackPanel>
                </Grid>
                <Grid Grid.Row="0" Grid.Column="2">
                    <StackPanel Orientation="Vertical" Width="80" Visibility="{Binding Source={x:Static p:Settings.Default}, Path=AccountWinrateVisibility, Converter={StaticResource CvtDataGridColumnVisibility}}">
                        <TextBlock Text="{DynamicResource DataGridHeaderStatisticsAccount}" HorizontalAlignment="Center" FontSize="15"/>
                    </StackPanel>
                </Grid>
            </Grid>
        </DataTemplate>
        <DataTemplate x:Key="StatisticsTemplateMirrored">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid Grid.Row="0" Grid.Column="0">
                    <StackPanel Orientation="Vertical" Width="80" Visibility="{Binding Source={x:Static p:Settings.Default}, Path=ShipWinrateVisibility, Converter={StaticResource CvtDataGridColumnVisibility}}">
                        <TextBlock Text="{Binding ShipBattles, Converter={StaticResource CvtBattleData}}" Foreground="{Binding Source={x:Static p:Settings.Default}, Path=ShipWinrateVisibility, Converter={StaticResource CvtDataGridColumnFadedColor}}" HorizontalAlignment="Center" FontSize="{Binding Source={x:Static p:Settings.Default}, Path=StatisticsColumnFontSize}" Margin="5,0,5,0"/>
                        <TextBlock Text="{Binding ShipWinrate, Converter={StaticResource CvtBattleData}, StringFormat={}{0:p2}}" Foreground="{Binding Source={x:Static p:Settings.Default}, Path=ShipWinrateVisibility, Converter={StaticResource CvtDataGridColumnFadedColor}}" HorizontalAlignment="Center" FontSize="{Binding Source={x:Static p:Settings.Default}, Path=StatisticsColumnFontSize}" Margin="5,0,5,0"/>
                        <TextBlock Text="{Binding ShipAvgExpPerBattle, Converter={StaticResource CvtBattleData}, StringFormat={}{0:f0}}" Foreground="{Binding Source={x:Static p:Settings.Default}, Path=ShipAvgExpVisibility, Converter={StaticResource CvtDataGridColumnFadedColor}}" HorizontalAlignment="Center" FontSize="{Binding Source={x:Static p:Settings.Default}, Path=StatisticsColumnFontSize}" Margin="5,0,5,0" Visibility="{Binding Source={x:Static p:Settings.Default}, Path=ShipAvgExpVisibility, Converter={StaticResource CvtDataGridColumnVisibility}}"/>
                        <TextBlock Text="{Binding ShipAvgDmgPerBattle, Converter={StaticResource CvtBattleData}, StringFormat={}{0:f0}}" Foreground="{Binding Source={x:Static p:Settings.Default}, Path=ShipAvgDmgVisibility, Converter={StaticResource CvtDataGridColumnFadedColor}}" HorizontalAlignment="Center" FontSize="{Binding Source={x:Static p:Settings.Default}, Path=StatisticsColumnFontSize}" Margin="5,0,5,0" Visibility="{Binding Source={x:Static p:Settings.Default}, Path=ShipAvgDmgVisibility, Converter={StaticResource CvtDataGridColumnVisibility}}"/>
                    </StackPanel>
                </Grid>
                <Grid Grid.Row="0" Grid.Column="1">
                    <StackPanel Orientation="Vertical" Width="80" Visibility="{Binding Source={x:Static p:Settings.Default}, Path=WeightedWinrateVisibility, Converter={StaticResource CvtDataGridColumnVisibility}}">
                        <TextBlock Text="" FontSize="{Binding Source={x:Static p:Settings.Default}, Path=StatisticsColumnFontSize}" Margin="5,0,5,0"/>
                        <TextBlock Text="{Binding WeightedWinrate, Converter={StaticResource CvtBattleData}, StringFormat={}{0:p2}}" Foreground="{Binding Source={x:Static p:Settings.Default}, Path=WeightedWinrateVisibility, Converter={StaticResource CvtDataGridColumnFadedColor}}" HorizontalAlignment="Center" FontSize="{Binding Source={x:Static p:Settings.Default}, Path=StatisticsColumnFontSize}" Margin="5,0,5,0"/>
                    </StackPanel>
                </Grid>
                <Grid Grid.Row="0" Grid.Column="2">
                    <StackPanel Orientation="Vertical" Width="80" Visibility="{Binding Source={x:Static p:Settings.Default}, Path=AccountWinrateVisibility, Converter={StaticResource CvtDataGridColumnVisibility}}">
                        <TextBlock Text="{Binding Battles, Converter={StaticResource CvtBattleData}}" Foreground="{Binding Source={x:Static p:Settings.Default}, Path=AccountWinrateVisibility, Converter={StaticResource CvtDataGridColumnFadedColor}}" HorizontalAlignment="Center" FontSize="{Binding Source={x:Static p:Settings.Default}, Path=StatisticsColumnFontSize}" Margin="5,0,5,0"/>
                        <TextBlock Text="{Binding AccountWinrate, Converter={StaticResource CvtBattleData}, StringFormat={}{0:p2}}" Foreground="{Binding Source={x:Static p:Settings.Default}, Path=AccountWinrateVisibility, Converter={StaticResource CvtDataGridColumnFadedColor}}" HorizontalAlignment="Center" FontSize="{Binding Source={x:Static p:Settings.Default}, Path=StatisticsColumnFontSize}" Margin="5,0,5,0"/>
                        <TextBlock Text="{Binding AvgExpPerBattle, Converter={StaticResource CvtBattleData}, StringFormat={}{0:f0}}" Foreground="{Binding Source={x:Static p:Settings.Default}, Path=AccountAvgExpVisibility, Converter={StaticResource CvtDataGridColumnFadedColor}}" HorizontalAlignment="Center" FontSize="{Binding Source={x:Static p:Settings.Default}, Path=StatisticsColumnFontSize}" Margin="5,0,5,0" Visibility="{Binding Source={x:Static p:Settings.Default}, Path=AccountAvgExpVisibility, Converter={StaticResource CvtDataGridColumnVisibility}}"/>
                    </StackPanel>
                </Grid>
            </Grid>
        </DataTemplate>

        <Style x:Key="TagCellStyle" TargetType="DataGridCell" BasedOn="{StaticResource {x:Type DataGridCell}}">
            <Setter Property="Visibility" Value="{Binding Source={x:Static p:Settings.Default}, Path=TagVisibility, Converter={StaticResource CvtDataGridColumnVisibility}}"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="VerticalContentAlignment" Value="Stretch"/>
            <Setter Property="Tag">
                <Setter.Value>
                    <MultiBinding Converter="{StaticResource CvtBackgroundColor}">
                        <Binding Path="AccountWinrate"/>
                        <Binding Path="WeightedWinrate"/>
                    </MultiBinding>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type DataGridCell}">
                        <Border x:Name="CellChrome"
                                Background="{Binding Tag, RelativeSource={RelativeSource TemplatedParent}}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="CellChrome" Property="BorderBrush" Value="{DynamicResource AppForegroundBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <DataGridTemplateColumn x:Key="AlliesNameColumn" HeaderTemplate="{StaticResource NameHeaderTemplate}" CellTemplate="{StaticResource NameTemplate}" Width="*" MinWidth="240"/>
        <DataGridTemplateColumn x:Key="AlliesStatisticsColumn" HeaderTemplate="{StaticResource StatisticsHeaderTemplate}" HeaderStyle="{StaticResource DataGridStatisticsColumnHeaderStyle}" CellTemplate="{StaticResource StatisticsTemplate}" Width="Auto" MinWidth="0" MaxWidth="240"/>
        <DataGridTemplateColumn x:Key="AlliesTagColumn" HeaderTemplate="{StaticResource TagHeaderTemplate}" CellTemplate="{StaticResource TagTemplate}" CellStyle="{StaticResource TagCellStyle}" Width="60" MinWidth="60" MaxWidth="60"/>
        <DataGridTemplateColumn x:Key="EnemiesNameColumn" HeaderTemplate="{StaticResource NameHeaderTemplate}" CellTemplate="{StaticResource NameTemplate}" Width="*" MinWidth="240"/>
        <DataGridTemplateColumn x:Key="EnemiesStatisticsColumn" HeaderTemplate="{StaticResource StatisticsHeaderTemplate}" HeaderStyle="{StaticResource DataGridStatisticsColumnHeaderStyle}" CellTemplate="{StaticResource StatisticsTemplate}" Width="Auto" MinWidth="0" MaxWidth="240"/>
        <DataGridTemplateColumn x:Key="EnemiesTagColumn" HeaderTemplate="{StaticResource TagHeaderTemplate}" CellTemplate="{StaticResource TagTemplate}" CellStyle="{StaticResource TagCellStyle}" Width="60" MinWidth="60" MaxWidth="60"/>

        <DataGridTemplateColumn x:Key="EnemiesNameColumnMirrored" HeaderTemplate="{StaticResource NameHeaderTemplate}" CellTemplate="{StaticResource NameTemplateMirrored}" Width="*" MinWidth="240">
            <DataGridTemplateColumn.HeaderStyle>
                <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource {x:Type DataGridColumnHeader}}">
                    <Setter Property="HorizontalContentAlignment" Value="Right"/>
                </Style>
            </DataGridTemplateColumn.HeaderStyle>
        </DataGridTemplateColumn>
        <DataGridTemplateColumn x:Key="EnemiesStatisticsColumnMirrored" HeaderTemplate="{StaticResource StatisticsHeaderTemplateMirrored}" HeaderStyle="{StaticResource DataGridStatisticsColumnHeaderMirroredStyle}" CellTemplate="{StaticResource StatisticsTemplateMirrored}" Width="Auto" MinWidth="0" MaxWidth="240"/>

        <ContextMenu x:Key="RowMenu" DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
            <ContextMenu.Resources>
                <Style TargetType="{x:Type MenuItem}" BasedOn="{StaticResource ThemedMenuItemStyle}"/>
            </ContextMenu.Resources>
            <MenuItem x:Name="ContextMenuCopyPlayerStatistics" Header="{DynamicResource ContextMenuCopyPlayerStatistics}" Click="ContextMenuCopyPlayerStatistics_Click" IsEnabled="{Binding Path=., Converter={StaticResource CvtContextMenuItemCopyPlayerStatisticsIsEnabled}}"/>
            <Separator/>
            <MenuItem x:Name="ContextMenuAddToWatchListPositive" Header="{DynamicResource ContextMenuAddToWatchListPositive}" Click="ContextMenuAddToWatchListPositive_Click" IsEnabled="{Binding Path=., Converter={StaticResource CvtContextMenuItemAddToWatchListPositiveIsEnabled}}"/>
            <MenuItem x:Name="ContextMenuAddToWatchListNegtive" Header="{DynamicResource ContextMenuAddToWatchListNegtive}" Click="ContextMenuAddToWatchListNegtive_Click" IsEnabled="{Binding Path=., Converter={StaticResource CvtContextMenuItemAddToWatchListNegtiveIsEnabled}}"/>
            <MenuItem x:Name="ContextMenuAddToWatchListCheater" Header="{DynamicResource ContextMenuAddToWatchListCheater}" Click="ContextMenuAddToWatchListCheater_Click" IsEnabled="{Binding Path=., Converter={StaticResource CvtContextMenuItemAddToWatchListCheaterIsEnabled}}"/>
            <MenuItem x:Name="ContextMenuRemoveFromWatchList" Header="{DynamicResource ContextMenuRemoveFromWatchList}" Click="ContextMenuRemoveFromWatchList_Click" IsEnabled="{Binding Path=., Converter={StaticResource CvtContextMenuItemRemoveFromWatchListIsEnabled}}"/>
            <Separator/>
            <MenuItem x:Name="ContextMenuCheckOnWoWSOfficialSite" Header="{DynamicResource ContextMenuCheckOnWoWSOfficialSite}" Click="ContextMenuCheckOnWoWSOfficialSite_Click" IsEnabled="{Binding Path=., Converter={StaticResource CvtContextMenuItemCheckOnWoWSOfficialSiteIsEnabled}}"/>
            <MenuItem x:Name="ContextMenuCheckOnWoWSNumbers" Header="{DynamicResource ContextMenuCheckOnWoWSNumbers}" Click="ContextMenuCheckOnWoWSNumbers_Click" IsEnabled="{Binding Path=., Converter={StaticResource CvtContextMenuItemCheckOnWoWSNumbersIsEnabled}}"/>
        </ContextMenu>

        <ToolTip x:Key="ToolTipPlayerDetails" Width="700">
            <ToolTip.Resources>
                <Style x:Key="PlayerDetailCellStyle" TargetType="TextBlock">
                    <Setter Property="FontSize" Value="{Binding Source={x:Static p:Settings.Default}, Path=DetailedStatisticsFontSize}"/>
                    <Setter Property="HorizontalAlignment" Value="Center"/>
                    <Setter Property="Margin" Value="4,5"/>
                </Style>
                <Style x:Key="PlayerDetailLabelStyle" TargetType="TextBlock" BasedOn="{StaticResource PlayerDetailCellStyle}">
                    <Setter Property="Foreground" Value="{DynamicResource AppMutedForegroundBrush}"/>
                    <Setter Property="HorizontalAlignment" Value="Left"/>
                </Style>
            </ToolTip.Resources>
            <Grid Width="676">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="1"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid Grid.Row="0" Margin="4,0,4,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" HorizontalAlignment="Center" FontWeight="SemiBold" FontSize="{Binding Source={x:Static p:Settings.Default}, Path=DetailedStatisticsFontSize}">
                        <Run Text="{Binding ClanTag, Converter={StaticResource CvtPlayerNamesVisibility}}"/> <Run Text="{Binding Name, Converter={StaticResource CvtPlayerNamesVisibility}}"/>
                    </TextBlock>
                    <TextBlock Grid.Column="1" HorizontalAlignment="Center" FontWeight="SemiBold" FontSize="{Binding Source={x:Static p:Settings.Default}, Path=DetailedStatisticsFontSize}">
                        <Run Text="{Binding ShipTier, Converter={StaticResource CvtShipTier}}"/> <Run Text="{Binding ShipName}"/>
                    </TextBlock>
                </Grid>
                <Border Grid.Row="1" Background="{DynamicResource AppGridLineBrush}"/>
                <Grid Grid.Row="2" Margin="0,8,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="92"/>
                        <ColumnDefinition Width="291"/>
                        <ColumnDefinition Width="1"/>
                        <ColumnDefinition Width="292"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="1" Text="{DynamicResource DataGridToolTipAccount}" Style="{StaticResource PlayerDetailCellStyle}" FontWeight="SemiBold"/>
                    <TextBlock Grid.Row="0" Grid.Column="3" Text="{DynamicResource DataGridToolTipShip}" Style="{StaticResource PlayerDetailCellStyle}" FontWeight="SemiBold"/>
                    <Border Grid.Row="0" Grid.RowSpan="6" Grid.Column="2" Background="{DynamicResource AppGridLineBrush}" Margin="0,2"/>

                    <UniformGrid Grid.Row="1" Grid.Column="1" Columns="3">
                        <TextBlock Text="{DynamicResource DataGridToolTipSolo}" Style="{StaticResource PlayerDetailLabelStyle}" HorizontalAlignment="Center"/>
                        <TextBlock Text="{DynamicResource DataGridToolTipDiv2}" Style="{StaticResource PlayerDetailLabelStyle}" HorizontalAlignment="Center"/>
                        <TextBlock Text="{DynamicResource DataGridToolTipDiv3}" Style="{StaticResource PlayerDetailLabelStyle}" HorizontalAlignment="Center"/>
                    </UniformGrid>
                    <UniformGrid Grid.Row="1" Grid.Column="3" Columns="3">
                        <TextBlock Text="{DynamicResource DataGridToolTipSolo}" Style="{StaticResource PlayerDetailLabelStyle}" HorizontalAlignment="Center"/>
                        <TextBlock Text="{DynamicResource DataGridToolTipDiv2}" Style="{StaticResource PlayerDetailLabelStyle}" HorizontalAlignment="Center"/>
                        <TextBlock Text="{DynamicResource DataGridToolTipDiv3}" Style="{StaticResource PlayerDetailLabelStyle}" HorizontalAlignment="Center"/>
                    </UniformGrid>

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="{DynamicResource DataGridToolTipBattles}" Style="{StaticResource PlayerDetailLabelStyle}"/>
                    <TextBlock Grid.Row="3" Grid.Column="0" Text="{DynamicResource DataGridToolTipWinrate}" Style="{StaticResource PlayerDetailLabelStyle}"/>
                    <TextBlock Grid.Row="4" Grid.Column="0" Text="{DynamicResource DataGridToolTipAvgExp}" Style="{StaticResource PlayerDetailLabelStyle}"/>
                    <TextBlock Grid.Row="5" Grid.Column="0" Text="{DynamicResource DataGridToolTipAvgDmg}" Style="{StaticResource PlayerDetailLabelStyle}"/>

                    <UniformGrid Grid.Row="2" Grid.Column="1" Columns="3">
                        <TextBlock Text="{Binding Battles_Solo, Converter={StaticResource CvtBattleData}}" Style="{StaticResource PlayerDetailCellStyle}"/>
                        <TextBlock Text="{Binding Battles_Div2, Converter={StaticResource CvtBattleData}}" Style="{StaticResource PlayerDetailCellStyle}"/>
                        <TextBlock Text="{Binding Battles_Div3, Converter={StaticResource CvtBattleData}}" Style="{StaticResource PlayerDetailCellStyle}"/>
                    </UniformGrid>
                    <UniformGrid Grid.Row="3" Grid.Column="1" Columns="3">
                        <TextBlock Text="{Binding AccountWinrate_Solo, Converter={StaticResource CvtBattleData}, StringFormat={}{0:p2}}" Style="{StaticResource PlayerDetailCellStyle}"/>
                        <TextBlock Text="{Binding AccountWinrate_Div2, Converter={StaticResource CvtBattleData}, StringFormat={}{0:p2}}" Style="{StaticResource PlayerDetailCellStyle}"/>
                        <TextBlock Text="{Binding AccountWinrate_Div3, Converter={StaticResource CvtBattleData}, StringFormat={}{0:p2}}" Style="{StaticResource PlayerDetailCellStyle}"/>
                    </UniformGrid>
                    <UniformGrid Grid.Row="4" Grid.Column="1" Columns="3">
                        <TextBlock Text="{Binding AvgExpPerBattle_Solo, Converter={StaticResource CvtBattleData}, StringFormat={}{0:f0}}" Style="{StaticResource PlayerDetailCellStyle}"/>
                        <TextBlock Text="{Binding AvgExpPerBattle_Div2, Converter={StaticResource CvtBattleData}, StringFormat={}{0:f0}}" Style="{StaticResource PlayerDetailCellStyle}"/>
                        <TextBlock Text="{Binding AvgExpPerBattle_Div3, Converter={StaticResource CvtBattleData}, StringFormat={}{0:f0}}" Style="{StaticResource PlayerDetailCellStyle}"/>
                    </UniformGrid>

                    <UniformGrid Grid.Row="2" Grid.Column="3" Columns="3">
                        <TextBlock Text="{Binding ShipBattles_Solo, Converter={StaticResource CvtBattleData}}" Style="{StaticResource PlayerDetailCellStyle}"/>
                        <TextBlock Text="{Binding ShipBattles_Div2, Converter={StaticResource CvtBattleData}}" Style="{StaticResource PlayerDetailCellStyle}"/>
                        <TextBlock Text="{Binding ShipBattles_Div3, Converter={StaticResource CvtBattleData}}" Style="{StaticResource PlayerDetailCellStyle}"/>
                    </UniformGrid>
                    <UniformGrid Grid.Row="3" Grid.Column="3" Columns="3">
                        <TextBlock Text="{Binding ShipWinrate_Solo, Converter={StaticResource CvtBattleData}, StringFormat={}{0:p2}}" Style="{StaticResource PlayerDetailCellStyle}"/>
                        <TextBlock Text="{Binding ShipWinrate_Div2, Converter={StaticResource CvtBattleData}, StringFormat={}{0:p2}}" Style="{StaticResource PlayerDetailCellStyle}"/>
                        <TextBlock Text="{Binding ShipWinrate_Div3, Converter={StaticResource CvtBattleData}, StringFormat={}{0:p2}}" Style="{StaticResource PlayerDetailCellStyle}"/>
                    </UniformGrid>
                    <UniformGrid Grid.Row="4" Grid.Column="3" Columns="3">
                        <TextBlock Text="{Binding ShipAvgExpPerBattle_Solo, Converter={StaticResource CvtBattleData}, StringFormat={}{0:f0}}" Style="{StaticResource PlayerDetailCellStyle}"/>
                        <TextBlock Text="{Binding ShipAvgExpPerBattle_Div2, Converter={StaticResource CvtBattleData}, StringFormat={}{0:f0}}" Style="{StaticResource PlayerDetailCellStyle}"/>
                        <TextBlock Text="{Binding ShipAvgExpPerBattle_Div3, Converter={StaticResource CvtBattleData}, StringFormat={}{0:f0}}" Style="{StaticResource PlayerDetailCellStyle}"/>
                    </UniformGrid>
                    <UniformGrid Grid.Row="5" Grid.Column="3" Columns="3">
                        <TextBlock Text="{Binding ShipAvgDmgPerBattle_Solo, Converter={StaticResource CvtBattleData}, StringFormat={}{0:f0}}" Style="{StaticResource PlayerDetailCellStyle}"/>
                        <TextBlock Text="{Binding ShipAvgDmgPerBattle_Div2, Converter={StaticResource CvtBattleData}, StringFormat={}{0:f0}}" Style="{StaticResource PlayerDetailCellStyle}"/>
                        <TextBlock Text="{Binding ShipAvgDmgPerBattle_Div3, Converter={StaticResource CvtBattleData}, StringFormat={}{0:f0}}" Style="{StaticResource PlayerDetailCellStyle}"/>
                    </UniformGrid>
                </Grid>
            </Grid>
        </ToolTip>
        <Style x:Key="DataGridPlayerListRowStyle" TargetType="DataGridRow" BasedOn="{StaticResource {x:Type DataGridRow}}">
            <Setter Property="ContextMenu" Value="{StaticResource RowMenu}"/>
            <Setter Property="ToolTip" Value="{StaticResource ToolTipPlayerDetails}"/>
        </Style>



    </Window.Resources>

    <Grid x:Name="MainWindowGrid" Background="{DynamicResource AppBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="84"/>
            <RowDefinition Height="39*" MinHeight="390"/>
            <RowDefinition Height="8*" MinHeight="80"/>
            <RowDefinition Height="60"/>
            <RowDefinition Height="60"/>
            <RowDefinition Height="10"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="16"/>
            <ColumnDefinition Width="90*" MinWidth="770"/>
            <ColumnDefinition Width="33*" MinWidth="330"/>
            <ColumnDefinition Width="16"/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="1" Margin="0,8,8,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Style="{StaticResource FluentElevatedSurfaceStyle}" Margin="0,0,4,0" Padding="12,8">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="2.4*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                            <TextBlock Text="{DynamicResource LabelAllies}" Style="{StaticResource FluentSectionHeaderStyle}"/>
                            <TextBlock Text="{DynamicResource LabelAverageData}" FontSize="{DynamicResource AppFontSizeCaption}" Foreground="{DynamicResource AppMutedForegroundBrush}" Margin="0,2,0,0"/>
                        </StackPanel>
                        <UniformGrid Grid.Column="1" Columns="3" HorizontalAlignment="Stretch" VerticalAlignment="Center">
                            <StackPanel>
                                <TextBlock Text="{DynamicResource LabelAverageAccountWinrate}" FontSize="{DynamicResource AppFontSizeCaption}" Foreground="{DynamicResource AppMutedForegroundBrush}" HorizontalAlignment="Center"/>
                                <TextBlock Text="{Binding AllyAvgAccountWinrate, StringFormat={}{0:P2}}" FontSize="{DynamicResource AppFontSizeSubtitle}" HorizontalAlignment="Center"/>
                            </StackPanel>
                            <StackPanel>
                                <TextBlock Text="{DynamicResource LabelAverageWeightedWinrate}" FontSize="{DynamicResource AppFontSizeCaption}" Foreground="{DynamicResource AppMutedForegroundBrush}" HorizontalAlignment="Center"/>
                                <TextBlock Text="{Binding AllyAvgWeightedWinrate, StringFormat={}{0:P2}}" FontSize="{DynamicResource AppFontSizeSubtitle}" HorizontalAlignment="Center"/>
                            </StackPanel>
                            <StackPanel>
                                <TextBlock Text="{DynamicResource LabelAverageBattleCount}" FontSize="{DynamicResource AppFontSizeCaption}" Foreground="{DynamicResource AppMutedForegroundBrush}" HorizontalAlignment="Center"/>
                                <TextBlock Text="{Binding AllyAvgBattleCount, StringFormat={}{0:F1}}" FontSize="{DynamicResource AppFontSizeSubtitle}" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </UniformGrid>
                    </Grid>
                </Border>
                <Border Grid.Column="1" Style="{StaticResource FluentElevatedSurfaceStyle}" Margin="4,0,0,0" Padding="12,8">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="2.4*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                            <TextBlock Text="{DynamicResource LabelEnemies}" Style="{StaticResource FluentSectionHeaderStyle}"/>
                            <TextBlock Text="{DynamicResource LabelAverageData}" FontSize="{DynamicResource AppFontSizeCaption}" Foreground="{DynamicResource AppMutedForegroundBrush}" Margin="0,2,0,0"/>
                        </StackPanel>
                        <UniformGrid Grid.Column="1" Columns="3" HorizontalAlignment="Stretch" VerticalAlignment="Center">
                            <StackPanel>
                                <TextBlock Text="{DynamicResource LabelAverageAccountWinrate}" FontSize="{DynamicResource AppFontSizeCaption}" Foreground="{DynamicResource AppMutedForegroundBrush}" HorizontalAlignment="Center"/>
                                <TextBlock Text="{Binding EnemyAvgAccountWinrate, StringFormat={}{0:P2}}" FontSize="{DynamicResource AppFontSizeSubtitle}" HorizontalAlignment="Center"/>
                            </StackPanel>
                            <StackPanel>
                                <TextBlock Text="{DynamicResource LabelAverageWeightedWinrate}" FontSize="{DynamicResource AppFontSizeCaption}" Foreground="{DynamicResource AppMutedForegroundBrush}" HorizontalAlignment="Center"/>
                                <TextBlock Text="{Binding EnemyAvgWeightedWinrate, StringFormat={}{0:P2}}" FontSize="{DynamicResource AppFontSizeSubtitle}" HorizontalAlignment="Center"/>
                            </StackPanel>
                            <StackPanel>
                                <TextBlock Text="{DynamicResource LabelAverageBattleCount}" FontSize="{DynamicResource AppFontSizeCaption}" Foreground="{DynamicResource AppMutedForegroundBrush}" HorizontalAlignment="Center"/>
                                <TextBlock Text="{Binding EnemyAvgBattleCount, StringFormat={}{0:F1}}" FontSize="{DynamicResource AppFontSizeSubtitle}" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </UniformGrid>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
        <Border Grid.Column="2" Style="{StaticResource FluentElevatedSurfaceStyle}" Margin="8,8,0,0" Padding="12,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Label Grid.Column="0" x:Name="LabelGamePathIsSetOrNot" Content="{Binding Source={x:Static p:Settings.Default}, Path=GamePath, Converter={StaticResource CvtGamePathIsSetText}}" Foreground="{Binding Source={x:Static p:Settings.Default}, Path=GamePath, Converter={StaticResource CvtGamePathIsSetColor}}" VerticalAlignment="Center" FontSize="{DynamicResource AppFontSizeSubtitle}"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Label Content="🌐" FontSize="18" VerticalAlignment="Center" Padding="0" Margin="0,0,6,0"/>
                    <ComboBox x:Name="ComboBoxLanguage" DisplayMemberPath="Content" SelectedValuePath="Value" VerticalAlignment="Center" Width="90" SelectionChanged="ComboBoxLanguage_SelectionChanged"/>
                </StackPanel>
            </Grid>
        </Border>
        <Border Grid.Row="1" Grid.Column="1" Grid.RowSpan="2"
                Style="{StaticResource FluentSurfaceStyle}"
                Margin="0,8,8,8"
                Padding="8">
            <Grid x:Name="PlayerListHost" VerticalAlignment="Stretch" SizeChanged="PlayerListHost_SizeChanged">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <DataGrid x:Name="DataGridAlliesList" Grid.Column="0" HorizontalAlignment="Stretch" Margin="0,0,4,0" VerticalAlignment="Stretch" ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.VerticalScrollBarVisibility="Disabled" AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" CanUserResizeRows="False" CanUserResizeColumns="False" SelectionMode="Single" HeadersVisibility="Column" ItemsSource="{Binding Allies}" CanUserReorderColumns="False" CanUserSortColumns="False" RowStyle="{StaticResource DataGridPlayerListRowStyle}"/>
                <DataGrid x:Name="DataGridEnemiesList" Grid.Column="1" HorizontalAlignment="Stretch" Margin="4,0,0,0" VerticalAlignment="Stretch" ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.VerticalScrollBarVisibility="Disabled" AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" CanUserResizeRows="False" CanUserResizeColumns="False" SelectionMode="Single" HeadersVisibility="Column" ItemsSource="{Binding Enemies}" CanUserReorderColumns="False" CanUserSortColumns="False" RowStyle="{StaticResource DataGridPlayerListRowStyle}"/>
            </Grid>
        </Border>
        <Border Grid.Row="1" Grid.Column="2"
                Style="{StaticResource FluentSurfaceStyle}"
                Margin="8,8,0,8"
                Padding="12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" MinHeight="58"/>
                    <RowDefinition Height="Auto" MinHeight="58"/>
                    <RowDefinition Height="3*"/>
                    <RowDefinition Height="1*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1.8*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,8,8">
                    <Label Content="{DynamicResource LabelDisplayStyle}" Padding="0"/>
                    <ComboBox x:Name="ComboBoxMirrored" DisplayMemberPath="Content" SelectedValuePath="Value" HorizontalAlignment="Stretch" SelectionChanged="ComboBoxMirrored_SelectionChanged"/>
                </StackPanel>
                <StackPanel Grid.Row="0" Grid.Column="1" Margin="8,0,0,8">
                    <Label Content="{DynamicResource LabelPlayerListSortBy}" Padding="0"/>
                    <ComboBox x:Name="ComboBoxSortBy" DisplayMemberPath="Content" SelectedValuePath="Value" HorizontalAlignment="Stretch" SelectionChanged="ComboBoxSortBy_SelectionChanged"/>
                </StackPanel>
                <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,8,8">
                    <Label Content="{DynamicResource LabelPlayerNames}" Padding="0"/>
                    <ComboBox x:Name="ComboBoxPlayerNamesVisibility" DisplayMemberPath="Content" SelectedValuePath="Value" HorizontalAlignment="Stretch" SelectionChanged="ComboBoxPlayerNamesVisibility_SelectionChanged"/>
                </StackPanel>
                <StackPanel Grid.Row="1" Grid.Column="1" Margin="8,0,0,8">
                    <Label Content="{DynamicResource LabelChartType}" Padding="0"/>
                    <ComboBox x:Name="ComboBoxChartType" DisplayMemberPath="Content" SelectedValuePath="Value" HorizontalAlignment="Stretch" SelectionChanged="ComboBoxChartType_SelectionChanged"/>
                </StackPanel>
                <Grid Grid.Row="2" Grid.ColumnSpan="2">
                    <lvc:CartesianChart x:Name="WinrateChart" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="{DynamicResource AppSurfaceBrush}" TooltipTextSize="13" TooltipFindingStrategy="CompareAllTakeClosest"/>
                </Grid>
                <Grid Grid.Row="3" Grid.ColumnSpan="2" Panel.ZIndex="1">
                    <lvc:CartesianChart x:Name="KDEChart" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="{DynamicResource AppSurfaceBrush}"/>
                </Grid>
            </Grid>
        </Border>

        <Border Grid.Row="2" Grid.Column="2"
                Style="{StaticResource FluentSurfaceStyle}"
                Margin="8,0,0,8"
                Padding="8">
            <TextBox x:Name="TxtOutputText" HorizontalAlignment="Stretch" TextWrapping="Wrap" VerticalAlignment="Stretch" VerticalScrollBarVisibility="Auto" IsEnabled="{Binding Source={x:Static p:Settings.Default}, Path=OutputTextUnlock}" FontSize="{Binding Source={x:Static p:Settings.Default}, Path=OutputTextFontSize}"/>
        </Border>
        <Border Grid.Row="3" Grid.Column="1" Grid.RowSpan="2"
                Style="{StaticResource FluentSurfaceStyle}"
                Margin="0,8,8,0"
                Padding="8"
                HorizontalAlignment="Left"
                VerticalAlignment="Top">
            <DataGrid x:Name="DataGridNotificationMessages" AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" CanUserResizeRows="False" CanUserResizeColumns="False" SelectionMode="Single" HeadersVisibility="Column" ItemsSource="{Binding Source={x:Static utils:NotificationMessageUtils.NotificationMessageCollection}}" CanUserReorderColumns="False" CanUserSortColumns="False" Height="84" HorizontalAlignment="Left" VerticalAlignment="Top">
                <DataGrid.Resources>
                    <DataTemplate x:Key="TimeHeaderTemplate">
                        <TextBlock Text="{DynamicResource DataGridHeaderTime}" FontSize="{DynamicResource AppFontSizeCaption}" FontWeight="SemiBold"/>
                    </DataTemplate>
                    <DataTemplate x:Key="TimeTemplate">
                        <TextBlock Text="{Binding Time, StringFormat='{}{0:yyyy-MM-dd HH:mm:ss}'}" FontSize="{DynamicResource AppFontSizeCaption}" Foreground="{Binding Type, Converter={StaticResource CvtNotificationMessageColor}}" Margin="5,1,0,1"/>
                    </DataTemplate>
                    <DataTemplate x:Key="MessageHeaderTemplate">
                        <TextBlock Text="{DynamicResource DataGridHeaderMessage}" FontSize="{DynamicResource AppFontSizeCaption}" FontWeight="SemiBold"/>
                    </DataTemplate>
                    <DataTemplate x:Key="MessageTemplate">
                        <TextBlock Text="{Binding Message}" FontSize="{DynamicResource AppFontSizeCaption}" Foreground="{Binding Type, Converter={StaticResource CvtNotificationMessageColor}}" Margin="5,1,0,1" TextTrimming="CharacterEllipsis" ToolTip="{Binding Message}"/>
                    </DataTemplate>
                </DataGrid.Resources>
                <DataGrid.Columns>
                    <DataGridTemplateColumn HeaderTemplate="{StaticResource TimeHeaderTemplate}" CellTemplate="{StaticResource TimeTemplate}" MinWidth="120"/>
                    <DataGridTemplateColumn HeaderTemplate="{StaticResource MessageHeaderTemplate}" CellTemplate="{StaticResource MessageTemplate}" MinWidth="570"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
        <Grid Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="2">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="BtnRefresh" Content="{DynamicResource BtnRefresh}" HorizontalAlignment="Left" Height="40" Margin="10,5,5,5" VerticalAlignment="Center" Width="80" Click="BtnRefresh_Click"/>
                <Button x:Name="BtnOpen" Content="{DynamicResource BtnOpen}" HorizontalAlignment="Left" Height="40" Margin="5" VerticalAlignment="Center" Width="80" Click="BtnOpen_Click"/>
                <Button x:Name="BtnConfig" Content="{DynamicResource BtnConfig}" HorizontalAlignment="Left" Height="40" Margin="5" VerticalAlignment="Center" Width="80" Click="BtnConfig_Click"/>
                <Button x:Name="BtnScreenshot" Content="{DynamicResource BtnScreenshot}" HorizontalAlignment="Left" Height="40" Margin="5" VerticalAlignment="Center" Width="80" Click="BtnScreenshot_Click"/>
            </StackPanel>
        </Grid>
        <Grid Grid.Row="4" Grid.Column="1" Grid.ColumnSpan="2">
            <StackPanel Orientation="Vertical" VerticalAlignment="Top" HorizontalAlignment="Right" Margin="0,0,10,0">
                <TextBlock HorizontalAlignment="Right" FontSize="11">
                    <Run Text="{DynamicResource WindowTitleApeRadar}"/>
                    <Run Text="{x:Static utils:BuildInfo.SoftwareVersion}"/>
                    <Run Text="Build"/>
                    <Run Text="{x:Static utils:BuildInfo.SoftwareDate}"/>
                </TextBlock>
                <TextBlock Text="{DynamicResource LabelAboutLine1}" HorizontalAlignment="Right" FontSize="11" Margin="0,5,0,5"/>
                <TextBlock HorizontalAlignment="Right" FontSize="11">
                    <Run Text="{DynamicResource LabelAboutLine2Part1}"/>
                    <Hyperlink x:Name="HyperLinkApeRadarWebsite" NavigateUri="https://lxdev.org/aperadar/" RequestNavigate="HyperLinkApeRadarWebsite_RequestNavigate">https://lxdev.org/aperadar/</Hyperlink>
                    <Run Text="{DynamicResource LabelAboutLine2Part2}"/>
                </TextBlock>
            </StackPanel>
        </Grid>
    </Grid>


</Window>
